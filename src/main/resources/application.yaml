server:
  port: ${SERVER_PORT:8080}

spring:
  application:
    name: blacklist-hub

  # R2DBC configuration (for runtime)
  r2dbc:
    url: ${SPRING_R2DBC_URL:r2dbc:mysql://localhost:3306/blacklist_hub}
    username: ${SPRING_R2DBC_USERNAME:root}
    password: ${SPRING_R2DBC_PASSWORD:root}
    pool:
      initial-size: 5
      max-size: 20
      max-idle-time: 30s

  # Flyway (migrations)
  flyway:
    enabled: true
    locations: classpath:db/migration
    url: ${SPRING_FLYWAY_URL:jdbc:mysql://localhost:3306/blacklist_hub}
    user: ${SPRING_FLYWAY_USER:root}
    password: ${SPRING_FLYWAY_PASSWORD:root}
    baseline-on-migrate: true

# Logging configuration
logging:
  level:
    root: INFO
    "[org.springframework.r2dbc]": WARN
    "[com.blacklisthub]": INFO

# Actuator (health, info, metrics)
management:
  endpoints:
    web:
      exposure:
        include: health, info, metrics
  endpoint:
    health:
      show-details: when_authorized
      probes:
        enabled: true

# Slack configuration
slack:
  app-token: ${SLACK_APP_TOKEN}
  bot-token: ${SLACK_BOT_TOKEN}
  signing-secret: ${SLACK_SIGNING_SECRET:}

app:
  allowed-channels: ${APP_ALLOWED_CHANNELS}
